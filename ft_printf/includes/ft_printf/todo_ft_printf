1/check validity

2/write vars (putnbr, putchar, putstr....)

types to handle
c => unsigned char
s => str
p => pointer in hexa
d, i => int
u => unsigned decimal
x,X => unsigned hexa (abcdef/ABCDEF)
% => writes a '%"
